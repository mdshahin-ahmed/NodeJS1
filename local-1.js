// module
// ->  A module is an "isolated" and "reusable" block of code that has it's "won scope"

// local module
// common js syntax

const add = (param1, param2) => param1 + param2;

const a = 10;

module.exports = { add, a };
// console.log(module);
